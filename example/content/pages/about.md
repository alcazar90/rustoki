---
title: "About"
---

This is a `pages/` file, not a `posts/` one — it renders at `/about` instead
of `/posts/about`, and doesn't show up in the post listing or the Atom feed.

Anything under `content/pages/` works this way: standalone content addressed
directly by URL.
